#we are creating tuple here 
#tuple cannot be changed or modify like lists, tuple are immutable 
tup = (1,2,3,"hey")  # firstly we have to know that we use curve brackets() to initialize it as a tuple

#now if we print the type of this tup we will get the <class 'tuple'>
print(type(tup))
print(tup)


#else all the methods of tuple is similar to list